# GitHub Release assets

Upload the APK and plugin files from this directory to the `v1.0.1` GitHub
Release. They are intentionally ignored by Git and should not be committed to
the repository history.
